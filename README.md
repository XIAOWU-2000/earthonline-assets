# 地球Online · 中国服 — 设计资产库

> **卦象是最古老的代码，太虚是最初的矩阵。**
> 黑客帝国说"现实由代码构成"，易经说"八卦是三位二进制"——同一件事的两个版本。

---

## ⚠️ 合规边界（符箓部分，必读）

符箓采用道教符箓的**视觉形式**，但做了明确的内容隔离。这是本项目能否上线的关键之一。

| ✅ 取（视觉形式） | ❌ 不取（宗教内容） |
|---|---|
| 三段式结构：符头 / 符胆 / 符脚 | 真实咒语文字 |
| 云篆的**笔画形态**（盘绕、回旋、似字非字） | "敕令"等宗教术语 |
| 朱砂 + 黄纸 + 墨的**配色** | 神明名讳 |
| 方形朱印的**形式** | 真实道观印文、可"启用"的符 |

**核心规则**：

> **符胆写的字，是产品的符名（如上善若水），不是道教咒文。**
> **法印内容是卦象与编号，不是道观印文。**

所以它是**"真符箓的样子"**，不是**"真符箓"**。

C 级禁用词（吉/凶/宜/忌/报应/业障/神仙/符咒/天机/注定/转运/改运/开光/消灾/驱邪/法事/供奉）在任何可见文案中均不得出现。

---

## 资产清单

### 01_符箓

| 文件 | 说明 |
|---|---|
| `talisman_zhusha.svg` | **朱砂黄纸**（传统样式）。黄纸底纹 + 朱砂渗染 + 手绘抖动滤镜 |
| `talisman_cyber.svg` | **赛博霓虹**（代码道场皮肤）。**与朱砂版共用同一套云篆路径**，仅换配色与表面处理 |

**两版共用结构**——这证明了皮肤系统可行：

```
符头 · 三勾（三道弧线 + 中轴）
  ↓
符胆 · 云篆「上善若水」四字（盘绕笔画，似字非字）
  ↓
法印 · 方形朱印（内嵌卦象 ☵ 坎）+ 编号 #023
  ↓
符脚 · 罡气盘绕 + 三重圈 + 收笔
```

**换画风的方法**：保留 `<path>` 结构，替换 `fill` / `stroke` 与滤镜，即得新皮肤。

规格：240 × 660（1:2.75）· 双线外框 · 顶部标签可删

### 02_劫怪

| 文件 | 说明 |
|---|---|
| `jieguai_style_sheet.svg` | 三只劫怪 + 画风要点：贪狼·薪 / 浑沌·怠 / 拖延兽 |

**《尸兄》画风执行要点**：

| 项 | 规格 |
|---|---|
| 轮廓 | **8–10pt 纯黑** `#0A0A0A` |
| 线条 | **必须抖动** —— `feTurbulence` + `feDisplacementMap`，scale 3–4.5 |
| 眼 | **一大一小**，不对称 |
| 局部红 | `#D93025`，**只做点缀**，不铺满 |
| 黏液 | 黄绿 `#A8C66C` |
| 禁忌 | **禁止可爱风、日系萌系** |

三只设计：

- **贪狼·薪**（#WRK-01 谈薪）：钱袋身 · 铜钱眼（方孔）· 锯齿尖牙 · 细爪抓硬币 · 贪狼小角
- **浑沌·怠**（#SEF-11 迷茫）：瘫软无定形 · 眯缝眼 · 无力下垂的嘴 · 漂浮红色问号
- **拖延兽**（#WRK-03 执行）：变形时钟身 · **断掉的指针** · 锁链缠身 · 歪斜刻度

### 03_交互框架

| 文件 | 说明 |
|---|---|
| `flow_interaction.svg` | 全流程 + 转场契约 + 硬约束 |
| `wireframe_components.svg` | 移动端三屏线框 + PC 三栏 + 组件九态 + 动效降级 |

**转场契约**：

| 跳转 | 动效 |
|---|---|
| P08 → P10 | 共享元素转场 200ms decelerate |
| P10 → P11 | 手牌扇形展开 stagger 60ms / 320ms |
| P11 → P12 | 故障 glitch 120ms + 面板上滑 |

**组件九态**：Default · Hover(PC) · Pressed · Focused(PC) · Disabled · Loading(骨架屏，不用转圈) · Empty · Error · Success

**三级降级**：high / medium / low —— 帧率 <50fps 自动降级，低端机必须能跑 low。

---

## 设计令牌（速查）

```
底色   #05070A / #0C1016 / #141A23 / #020305
霓虹   青#00F0FF · 品红#FF003C · 黄#FCEE0A · 矩阵绿#00FF41
五行   金#D8DEE9 木#00E5A0 水#2E7CF6 火#FF3B30 土#F5C518
文本   #E8EDF5 / #9AA5B4 / #5A6472

字体   JetBrains Mono（代码数值）· Noto Sans SC（中文）
字重   400 正文 / 500 次级 / 700 标题数值
行高   正文 1.6（暗底比浅底耗眼力）

动效   fast 120ms · base 200ms · slow 320ms
```

---

## 使用方式

SVG 可直接用于：

- **Flutter**：`flutter_svg` 加载
- **Web / 设计稿**：直接引用
- **换皮肤**：替换 `fill` / `stroke` 与 `filter`，保留 `path` 结构

> GitHub 可直接预览 SVG。点击文件即可查看渲染效果。

---

*资产库 v1 · 2026-09-02*
